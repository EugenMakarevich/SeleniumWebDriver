# Environment
driver.strategy=${driver.strategy}
driver.browser=${driver.browser}
driver.browser.version=${driver.browser.version}
driver.platform=${driver.platform}
driver.grid.hub.url=${driver.grid.hub.url}
